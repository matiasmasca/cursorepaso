<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

        <ul>
        <% city.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, "Nombre de la ciudad", class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :city_population, "Población Total", class: "form-label" %>
    <%= form.number_field :city_population, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :province_id, "Cod. Provincia", class: "form-label" %>
    <%= form.text_field :province_id, class: "form-control", required: true  %>
  </div>

  <div class="form-group actions">
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>
<% end %>
